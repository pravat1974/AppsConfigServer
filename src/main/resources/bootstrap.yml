spring:
  cloud:
    config:
      server: 
        git:
          uri:https://github.com/pravat1974/AppConfigData
          clone-on-start:true
          skip-ssl-validation:true
          timeout:400
          username: ---
          password: --
 
  application:
    name:config-server
server:
  port: 8090
  address:localhost
  
 

